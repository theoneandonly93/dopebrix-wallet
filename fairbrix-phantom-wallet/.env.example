########################################
# Client (VITE_*) and Server env (API) #
########################################

# Network label in the UI
VITE_NETWORK_NAME=fairbrix
VITE_ICON_VER=1

# Prefer proxy on web/mobile; dev desktop can hit local RPC
VITE_FAIRBRIX_PROXY_URL=/api/rpc
VITE_FAIRBRIX_BROADCAST_URL=/api/broadcast
VITE_FAIRBRIX_API_TOKEN=

# Local RPC (desktop/electron or dev)
VITE_FAIRBRIX_RPC_URL=http://127.0.0.1:8645
VITE_FAIRBRIX_RPC_USER=1
VITE_FAIRBRIX_RPC_PASS=1
VITE_FAIRBRIX_WALLET_NAME=dope
# Optional: auto‑unlock if wallet is encrypted
VITE_FAIRBRIX_WALLET_PASSPHRASE=

# Integrations
VITE_SWAP_API_URL=
VITE_ORDINALS_API_URL=
VITE_RUNES_API_URL=
VITE_INDEXER_API_URL=https://fairbrixscan.com
VITE_BUY_URL=
VITE_STAKING_API_URL=

# DOPE (L2) balances via proxy
VITE_DOPE_API_URL=
VITE_SHOW_EMPTY_TOKENS=true
VITE_DOPE_PRICE_USD=0.01

# Price endpoint used in client
VITE_PRICE_API_URL=/api/price

# Address format overrides (advanced)
# VITE_FAIRBRIX_PKH=0x5f
# VITE_FAIRBRIX_WIF=0x80
# VITE_FAIRBRIX_COMPRESSED=true

# Optional Fairbrix-pack launcher
# USE_FAIRBRIX_PACK=1
# FAIRBRIX_PACK_DIR=C:\\path\\to\\Fairbrix-pack
# FAIRBRIX_PACK_EXE=C:\\path\\to\\Fairbrix-pack\\START_Fairbrix.exe

########################################
# Serverless (Vercel) API settings     #
########################################

# Leave blank to avoid requiring Authorization while testing
API_JWT_SECRET=
ALLOWED_ORIGINS=http://localhost:5173, http://127.0.0.1:5173, https://*.vercel.app, https://dopebrix.com, https://www.dopebrix.com

# Upstream Fairbrix node for /api/rpc and /api/broadcast
# Use your HTTPS proxy host (recommended) or public node IP:port
UPSTREAM_RPC_URL=
UPSTREAM_RPC_USER=
UPSTREAM_RPC_PASS=

# Server-side price/indexer fallbacks
INDEXER_API_URL=https://fairbrixscan.com
# PRICE_FBX_USD=0.01
# PRICE_FEED_URL=https://your-feed.example.com/fbx-usd

# Optional: limit proxy RPC methods (comma-separated). Defaults are safe.
# RPC_ALLOWED_METHODS=getblockcount,getblockhash,getblock,getbalance,getwalletinfo,getblockchaininfo,getnewaddress,getaddressesbylabel,validateaddress,getaddressinfo,listunspent,getrawtransaction,createrawtransaction,fundrawtransaction,signrawtransactionwithwallet,signmessage,walletpassphrase,sendrawtransaction,sendtoaddress,importprivkey,loadwallet,createwallet,setgenerate,stake

########################################
# DOPE L2 (serverless proxy → reward)  #
########################################
# The app's /api/dope forwards here (to your reward server)
DOPE_API_URL=
# Must match the reward server's HMAC_SECRET
DOPE_HMAC_SECRET=
# Optional bearer if your reward server enforces it
DOPE_API_TOKEN=

